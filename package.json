{
  "name": "cf-markdown",
  "version": "1.0.0",
  "description": "Markdown conversion tool using Cloudflare Workers AI",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc && npm run copy-static",
    "copy-static": "node scripts/copy-static.js",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler pages deploy public",
    "test": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250115.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8",
    "wrangler": "^3.103.1"
  }
}
